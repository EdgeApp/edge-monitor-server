# edge-monitor-server

## Unreleased

- added: WebSocket plugin for monitoring change-server connections
- changed: Engine now runs fixtures in parallel independent loops (prevents blocking on timeouts)
